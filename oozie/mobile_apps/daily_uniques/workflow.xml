<workflow-app name="mobile_apps_daily_uniques-${year}-${month}-${day}-wf" xmlns="uri:oozie:workflow:0.4">
    <parameters>
        <!-- Required properties -->
        <property><name>name_node</name></property>
        <property><name>job_tracker</name></property>

        <property>
            <name>hive_site_xml</name>
            <description>hive-site.xml file path in HDFS</description>
        </property>
        <!-- specifying parameter values in file to test running -->
        <property>
            <name>source_table</name>
            <description>
                Hive table to read data from.
            </description>
        </property>
        <property>
            <name>year</name>
            <description>The partition's year</description>
        </property>
        <property>
            <name>month</name>
            <description>The partition's month</description>
        </property>
        <property>
            <name>day</name>
            <description>The partition's day</description>
        </property>
        <property>
            <name>temporary_directory</name>
            <value>${temporary_directory}</value>
        </property>
        <property>
            <name>mark_directory_done_workflow_file</name>
            <description>Workflow for marking a directory done</description>
        </property>
        <property>
            <name>archive_job_output_workflow_file</name>
            <description>Workflow to move a data file to the archive</description>
        </property>
    </parameters>

    <start to="get_apps_uniques"/>
    <action name="get_apps_uniques">
        <hive xmlns="uri:oozie:hive-action:0.2">
            <job-tracker>${job_tracker}</job-tracker>
            <name-node>${name_node}</name-node>
            <job-xml>${hive_site_xml}</job-xml>
            <configuration>
                <property>
                    <name>mapreduce.job.queuename</name>
                    <value>${queue_name}</value>
                </property>
                <property>
                    <name>hive.exec.scratchdir</name>
                    <value>/tmp/hive-${user}</value>
                </property>
            </configuration>
            <script>generate_daily_uniques.hql</script>
            <param>source_table=${source_table}</param>
            <param>destination_directory=${temporary_directory}/${wf:id()}</param>
            <param>year=${year}</param>
            <param>month=${month}</param>
            <param>day=${day}</param>
        </hive>
        <ok to="mark_dataset_done"/>
        <error to="kill"/>
    </action>
    <kill name="kill">
        <message>Action failed, error message[${wf:errorMessage(wf:lastErrorNode())}]</message>
    </kill>

    <action name="mark_dataset_done">
        <sub-workflow>
            <app-path>${mark_directory_done_workflow_file}</app-path>
            <configuration>
                <property>
                    <name>directory</name>
                    <value>${temporary_directory}/${wf:id()}</value>
                </property>
            </configuration>
        </sub-workflow>
        <ok to="move_apps_uniques_to_archive"/>
        <error to="kill"/>
    </action>


<action name="move_apps_uniques_to_archive">
    <sub-workflow>
            <app-path>${archive_job_output_workflow_file}</app-path>
            <configuration>
                <property>
                    <name>source_directory</name>
                    <value>${temporary_directory}/${wf:id()}</value>
                </property>
                <property>
                    <name>expected_filename_ending</name>
                    <value>gz</value>
                </property>
                <property>
                    <name>archive_file</name>
                    <!-- not sure if hardcoding  filename here is right-->
                    <value>${archive_directory}/mobile_apps/uniques/daily/${year}-${month}-${day}.gz</value>
                </property>
                <property>
                    <name>name_node</name>
                    <value>${name_node}</value>
                </property>
                <property>
                    <name>job_tracker</name>
                    <value>${job_tracker}</value>
                </property>

                <property>
                    <name>hive_site_xml</name>
                    <value>${hive_site_xml}</value>
                </property>

            </configuration>
        </sub-workflow>
        <ok to="end"/>
        <error to="archive_fail"/>
    </action>

 <kill name="archive_fail">
    <message>Archive of mobile apps daily unques failed, error
            message[${wf:errorMessage(wf:lastErrorNode())}]</message>
 </kill>

    <end name="end"/>
</workflow-app>
