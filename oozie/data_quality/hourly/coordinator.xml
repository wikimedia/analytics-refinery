<?xml version="1.0" encoding="UTF-8"?>
<coordinator-app xmlns="uri:oozie:coordinator:0.4"
    xmlns:sla="uri:oozie:sla:0.2"
    name="data-quality-${source_table}-${query_name}-hourly-coord"
    frequency="${coord:hours(1)}"
    start="${start_time}"
    end="${stop_time}"
    timezone="Universal">

    <parameters>
        <property><name>name_node</name></property>
        <property><name>job_tracker</name></property>
        <property><name>hive2_jdbc_url</name></property>
        <property><name>hive_site_xml</name></property>
        <property><name>queue_name</name></property>
        <property><name>user</name></property>
        <property><name>artifacts_directory</name></property>
        <property><name>refinery_jar_version</name></property>

        <property><name>source_table</name></property>
        <property><name>source_directory</name></property>
        <property><name>data_quality_table</name></property>
        <property><name>query_name</name></property>
        <property><name>start_time</name></property>
        <property><name>stop_time</name></property>
        <property><name>workflow_file</name></property>

        <property><name>flag_directory_done_workflow_file</name></property>
        <property><name>done_flag_destination_directory</name> </property>
        <property><name>send_error_email_workflow_file</name></property>
        <property><name>sla_alert_contact</name></property>
    </parameters>

    <controls>
        <timeout>-1</timeout>
        <concurrency>2</concurrency>
        <throttle>2</throttle>
    </controls>

    <datasets>
        <dataset name="source_dataset"
                 frequency="${coord:hours(1)}"
                 initial-instance="${start_time}"
                 timezone="Universal">
            <uri-template>${source_directory}/year=${YEAR}/month=${"$"}{MONTH + 0}/day=${"$"}{DAY + 0}/hour=${"$"}{HOUR + 0}</uri-template>
            <done-flag>${done_flag}</done-flag>
        </dataset>

        <dataset name="data_quality_hourly"
                 frequency="${coord:hours(1)}"
                 initial-instance="${start_time}"
                 timezone="Universal">
             <uri-template>${data_quality_directory}/source_table=${source_table}/query_name=${query_name}/year=${YEAR}/month=${"$"}{MONTH + 0}/day=${"$"}{DAY + 0}/hour=${"$"}{HOUR + 0}</uri-template>
             <done-flag>_SUCCESS</done-flag>
        </dataset>
    </datasets>

    <input-events>
        <data-in name="input" dataset="source_dataset">
            <instance>${coord:current(0)}</instance>
        </data-in>
    </input-events>

    <output-events>
        <data-out name="output" dataset="data_quality_hourly">
            <instance>${coord:current(0)}</instance>
        </data-out>
    </output-events>

    <action>
        <workflow>
            <app-path>${workflow_file}</app-path>
            <configuration>
                <property>
                    <name>year</name>
                    <value>${coord:formatTime(coord:nominalTime(), "y")}</value>
                </property>
                <property>
                    <name>month</name>
                    <value>${coord:formatTime(coord:nominalTime(), "M")}</value>
                </property>
                <property>
                    <name>day</name>
                    <value>${coord:formatTime(coord:nominalTime(), "d")}</value>
                </property>
                <property>
                    <name>hour</name>
                    <value>${coord:formatTime(coord:nominalTime(), "H")}</value>
                </property>
                <property>
                    <name>done_flag_destination_directory</name>
                    <value>${coord:dataOut('output')}</value>
                </property>
            </configuration>
        </workflow>

        <sla:info>
            <!--
                Use action actual time as SLA base, since it's the time used
                to compute timeout
                We use 6 hours as the maximum waiting time for hourly data is 6 hours
            -->
            <sla:nominal-time>${coord:actualTime()}</sla:nominal-time>
            <sla:should-end>${6 * HOURS}</sla:should-end>
            <sla:alert-events>end_miss</sla:alert-events>
            <sla:alert-contact>${sla_alert_contact}</sla:alert-contact>
        </sla:info>
    </action>
</coordinator-app>
