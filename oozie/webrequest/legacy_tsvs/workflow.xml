<?xml version="1.0" encoding="UTF-8"?>
<workflow-app xmlns="uri:oozie:workflow:0.4"
    name="webrequest-legacy_tsvs-wf-${aspect_name}-${year}-${month}-${day}">

    <parameters>

        <property>
            <name>aspect_compression_ending</name>
            <value>.gz</value>
            <description>The filename ending for this aspect's file.</description>
        </property>

        <!-- Default values for inner oozie settings -->
        <property>
            <name>oozie_launcher_queue_name</name>
            <value>${queue_name}</value>
        </property>
        <property>
            <name>oozie_launcher_memory</name>
            <value>256</value>
        </property>

        <!-- Required properties -->
        <property><name>queue_name</name></property>
        <property><name>name_node</name></property>
        <property><name>job_tracker</name></property>

        <property>
            <name>hive_site_xml</name>
            <description>hive-site.xml file path in HDFS</description>
        </property>
        <property>
            <name>artifacts_directory</name>
            <description>Path in HDFS to artifacts.  refinery-hive.jar should be here.</description>
        </property>
        <property>
            <name>webrequest_table</name>
            <description>
                Hive table to read webrequest data from.
            </description>
        </property>
        <property>
            <name>year</name>
            <description>The partition's year</description>
        </property>
        <property>
            <name>month</name>
            <description>The partition's month</description>
        </property>
        <property>
            <name>day</name>
            <description>The partition's day</description>
        </property>
        <property>
            <name>mark_directory_done_workflow_file</name>
            <description>Workflow for marking a directory done</description>
        </property>
        <property>
            <name>temporary_directory</name>
            <description>A directory in HDFS for temporary files</description>
        </property>
        <property>
            <name>aspect_name</name>
            <description>Aspect to cover. Currently, only "zero".</description>
        </property>
        <property>
            <name>aspect_tsv_archive_directory</name>
            <description>Directory to archive the generated tsv in</description>
        </property>
        <property>
            <name>archive_job_output_workflow_file</name>
            <description>Workflow to move a data file to the archive</description>
        </property>
        <property>
            <name>send_error_email_workflow_file</name>
            <description>Workflow for sending an error email</description>
        </property>
    </parameters>

    <start to="extract_data_into_single_file"/>

    <action name="extract_data_into_single_file">
        <hive xmlns="uri:oozie:hive-action:0.3">
            <job-tracker>${job_tracker}</job-tracker>
            <name-node>${name_node}</name-node>
            <job-xml>${hive_site_xml}</job-xml>
            <configuration>
                <!--make sure oozie:launcher runs in a low priority queue -->
                <property>
                    <name>oozie.launcher.mapred.job.queue.name</name>
                    <value>${oozie_launcher_queue_name}</value>
                </property>
                <property>
                    <name>oozie.launcher.mapreduce.map.memory.mb</name>
                    <value>${oozie_launcher_memory}</value>
                </property>

                <property>
                    <name>mapreduce.job.queuename</name>
                    <value>${queue_name}</value>
                </property>
                <property>
                    <!--
                    Doubling available heap for maps from cluster-wide default
                    of 592MB to 1184MB, as otherwise the 5xx tsvs run out of
                    heap space.
                    -->
                    <name>mapreduce.map.java.opts</name>
                    <value>-Xmx1184m</value>
                </property>
            </configuration>

            <script>generate_${aspect_name}_tsv.hql</script>

            <param>artifacts_directory=${artifacts_directory}</param>
            <param>webrequest_table=${webrequest_table}</param>
            <param>destination_directory=${temporary_directory}/${wf:id()}-${aspect_name}</param>
            <param>year=${year}</param>
            <param>month=${month}</param>
            <param>day=${day}</param>
            <!--
            We extract the needed webrequest_sources from the coordinator path
            (which always exists in our setting).
            This variable holds quoted strings, separated by commas. So for
            example a coordinator path of

              /path/to/coordinator_misc_text.xml

            will get turned into

              'misc', 'text'

            . This allows to use the variable straight in HivQL via

              [...] AND webrequest_source IN ( ${webrequest_source} )

            and not worry about duplicating the dependencies between the
            coordinator and the HiveQL file.
            -->
            <param>webrequest_sources=${replaceAll(replaceAll(wf:conf('oozie.coord.application.path'), '^.*/coordinator_([a-z_]*).xml', '\'$1\''),'_','\', \'')}</param>
        </hive>
        <ok to="mark_dataset_done"/>
        <error to="send_error_email"/>
    </action>

    <action name="mark_dataset_done">
        <sub-workflow>
            <app-path>${mark_directory_done_workflow_file}</app-path>
            <configuration>
                <property>
                    <name>directory</name>
                    <value>${temporary_directory}/${wf:id()}-${aspect_name}</value>
                </property>
            </configuration>
        </sub-workflow>
        <ok to="move_data_to_archive"/>
        <error to="send_error_email"/>
    </action>

    <action name="move_data_to_archive">
        <sub-workflow>
            <app-path>${archive_job_output_workflow_file}</app-path>
            <propagate-configuration/>
            <configuration>
                <property>
                    <name>source_directory</name>
                    <value>${temporary_directory}/${wf:id()}-${aspect_name}</value>
                </property>
                <property>
                    <name>expected_filename_ending</name>
                    <value>${aspect_compression_ending}</value>
                </property>
                <property>
                    <name>archive_file</name>
                    <value>${aspect_tsv_archive_directory}/${aspect_name}.tsv.log-${year}${month}${day}${aspect_compression_ending}</value>
                </property>
            </configuration>
        </sub-workflow>
        <ok to="end"/>
        <error to="send_error_email"/>
    </action>

    <action name="send_error_email">
        <sub-workflow>
            <app-path>${send_error_email_workflow_file}</app-path>
            <propagate-configuration/>
            <configuration>
                <property>
                    <name>parent_name</name>
                    <value>${wf:name()}</value>
                </property>
                <property>
                    <name>parent_failed_action</name>
                    <value>${wf:lastErrorNode()}</value>
                </property>
                <property>
                    <name>parent_error_code</name>
                    <value>${wf:errorCode(wf:lastErrorNode())}</value>
                </property>
                <property>
                    <name>parent_error_message</name>
                    <value>${wf:errorMessage(wf:lastErrorNode())}</value>
                </property>
            </configuration>
        </sub-workflow>
        <ok to="kill"/>
        <error to="kill"/>
    </action>

    <kill name="kill">
        <message>Action failed, error message[${wf:errorMessage(wf:lastErrorNode())}]</message>
    </kill>
    <end name="end"/>
</workflow-app>
